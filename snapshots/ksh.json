[
  {
    "name": "ksh",
    "options": [
      {
        "type": "short",
        "key": "c",
        "title": "-c\n    string",
        "description": "ksh will execute the command(s) contained in\n      string."
      },
      {
        "type": "short",
        "key": "i",
        "title": "-i",
        "description": "Interactive shell. A shell is “interactive” if this option\n      is used or if both standard input and standard error are attached to a\n      tty(4). An interactive shell has job\n      control enabled, ignores the SIGINT,\n      SIGQUIT, and SIGTERM\n      signals, and prints prompts before reading input (see the\n      PS1 and PS2 parameters).\n      For non-interactive shells, the trackall option is\n      on by default (see the set command below)."
      },
      {
        "type": "short",
        "key": "l",
        "title": "-l",
        "description": "Login shell. If the basename the shell is called with (i.e. argv[0])\n      starts with ‘-’ or if this option is\n      used, the shell is assumed to be a login shell and the shell reads and\n      executes the contents of /etc/profile and\n      $HOME/.profile if they exist and are\n    readable."
      },
      {
        "type": "short",
        "key": "p",
        "title": "-p",
        "description": "Privileged shell. A shell is “privileged” if this option is\n      used or if the real user ID or group ID does not match the effective user\n      ID or group ID (see getuid(2) and\n      getgid(2)). A privileged shell does not\n      process $HOME/.profile nor the\n      ENV parameter (see below). Instead, the file\n      /etc/suid_profile is processed. Clearing the\n      privileged option causes the shell to set its effective user ID (group ID)\n      to its real user ID (group ID)."
      },
      {
        "type": "short",
        "key": "r",
        "title": "-r",
        "description": "Restricted shell. A shell is “restricted” if this option is\n      used; if the basename the shell was invoked with was “rksh”;\n      or if the SHELL parameter is set to\n      “rksh”. The following restrictions come into effect after\n      the shell processes any profile and ENV files:\n    \n    \n      The cd command is disabled.\n      The SHELL, ENV, and\n          PATH parameters cannot be changed.\n      Command names can't be specified with absolute or relative paths.\n      The -p option of the built-in command\n          command can't be used.\n      Redirections that create files can't be used (i.e.\n          ‘>’,\n          ‘>|’,\n          ‘>>’,\n          ‘<>’)."
      },
      {
        "type": "short",
        "key": "s",
        "title": "-s",
        "description": "The shell reads commands from standard input; all non-option arguments are\n      positional parameters."
      }
    ]
  }
]
